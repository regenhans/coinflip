## Random Number Generator using a coin flip

A simple random number generator, using jest for testing.

### Requirements: 

* Node js

### To run the test:

* Copy the repo
#### Install dependencies
* `npm install`
#### Run test
* `npm test`